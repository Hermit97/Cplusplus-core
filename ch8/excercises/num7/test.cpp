#include <iostream>
#include <vector>
#include <algorithm>

/*Get name and age. Then print them both side by side. Take 5 names/ages*/
std::vector<std::string> names;
std::vector<int> ages;

// Get names
void getNames(std::vector<std::string>& n){
    std::string name;
    std::cin >> name;
}
